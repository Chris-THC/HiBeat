import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  playlistCard: {
    height: 230,
    width: 180,
    flexDirection: 'column',
    alignItems: 'center',
  },
  imgPlaylist: {
    height: 170,
    width: 170,
    borderRadius: 8,
  },
  textCard: {
    color: '#ccc',
    fontSize: 16,
    textAlign: 'center',
    marginTop: 8,
  },
});
